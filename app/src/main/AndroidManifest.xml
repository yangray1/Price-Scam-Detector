<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.pricescamdetector">

    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-feature android:name="android.hardware.camera"/>
    <uses-feature android:name="android.hardware.camera.autofocus"/>
    <uses-permission android:name="android.permission.INTERNET"/>


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning"
        android:usesCleartextTraffic="true">
        <activity

            android:name="com.example.pricescamdetector.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar"> <!-- Sets no action bar -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Add mobile vision dependencies versions to metadata -->
        <meta-data
            android:name="com.google.android.gms.vision.DEPENDENCIES"
            android:value="barcode">
        </meta-data>

        <activity android:name="com.example.pricescamdetector.ScanBarcodeActivity"
            android:label="Scan Barcode"
            android:parentActivityName="com.example.pricescamdetector.MainActivity"/>

        <activity android:name="com.example.pricescamdetector.ProductListActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
        </activity>

        <activity android:name="com.example.pricescamdetector.RowLayoutActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
        </activity>

        <activity android:name="com.example.pricescamdetector.WebViewActivity"
            android:label="@string/app_name"
            android:parentActivityName="com.example.pricescamdetector.ProductListActivity">
        </activity>
        <uses-library android:name="org.apache.http.legacy" android:required="false"/>
    </application>

</manifest>